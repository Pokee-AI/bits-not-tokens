# v2: held-out test of the x-axis claim

v2 was not issued as a full brief. After v1 failed, the decision below was given verbatim;
its operational content became `PASS_CRITERIA_v2.md` (committed before any v2 run) and the
v2 section of `RESULTS.md`.

---

Decision: the post's claim is the x-axis claim (H2 family). Do the following in order.

1. Leave PASS_CRITERIA.md unchanged. Record the v1 outcome (H1 FAIL, H2 FAIL) in RESULTS.md as is.
2. Create PASS_CRITERIA_v2.md and commit it BEFORE launching anything:
   - Freeze n0 at the value fitted on the existing Zipf runs only (exclude EQ4). State the value.
   - If a single n0 is clearly inadequate across filler levels, freeze one n0 per filler level
     (n=0, n=64) from existing runs, and say so. No other free parameters.
   - Held-out corpora: EQ16_n0, EQ16_n64, EQ32_n0, seeds 1 and 2, same model and LR.
     Use a fact subset if needed to keep each run under ~3 hours; state K.
   - Targets T: choose three values of facts stored that every EXISTING corpus reaches
     (from current CSVs). Fix them now.
   - H2v2 PASS if, at the largest T, the held-out EQ curves fall within 2x (in I_eff)
     of the range spanned by the Zipf curves, with n0 frozen. Report spread in tokens
     and in I_eff over all corpora including EQ.
   - H1 becomes descriptive: report fitted beta per corpus next to the ideal-learner beta
     over the same range. No pass/fail.
3. Run the held-out corpora. Do not refit n0 afterwards; if you also report a refit, label it exploratory.
4. Add to RESULTS.md: fraction of delivered facts stored vs number of exposures (binned by n_k),
   per corpus. This is the absorption curve and is likely the most informative plot we have.

---

Implementer's notes adopted: the single n0 = 9.88 was frozen (per-filler fits differ but sit on
a residual plateau); T = {100, 200, 300} because EQ4 never exceeds chance-correction noise; the
outcome was predicted and recorded before the runs (`DEVIATIONS.md` #11).
